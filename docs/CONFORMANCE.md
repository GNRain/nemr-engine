# Conformance Matrix

Traceability from every NEMR-SPEC-001 requirement to the code that implements it
and the test that proves it, plus the ledger of audit findings and their
disposition. Kept current from WP A onward.

**How to read the status column**

| Status | Meaning |
|---|---|
| ✅ done | Implemented and covered by a test that would catch a regression. |
| 🟡 partial | Implemented, but the test is a smoke-script step or missing; a dedicated test is owed. |
| 🔶 drifted | Code does something reasonable the spec no longer describes; resolution noted. |
| ⛔ absent | Not implemented (or a stub). |

**Test locations.** Unit tests live beside their code (`#[cfg(test)]`). Host-backed
regression tests are in `tests/regression.rs`, driven by `tests/common/mod.rs`,
which **refuses to skip** — a missing host facility fails the test with
instructions rather than silently passing (this is why the old `#[ignore]`d
VOL-06 suite never ran). The end-to-end script is `scripts/e2e_smoke_test.sh`.

**What CI proves, and what it does not.** All five CI jobs are green on a clean
Ubuntu runner, including the host-backed regression suite (10/10, hash-gated) and
the E2E smoke test. Two limits are deliberate and must not be blurred by a green
badge:

- CI runs the smoke test with `NEMR_SKIP_API=1`. It never contacts the Anthropic
  API. Only a local run exercises the round-trip.
- CI provisions a **placeholder** credential. That proves AUTH-02's mount
  mechanics — bind-mounted, read-only, at the path Claude Code expects — and
  proves nothing about whether a real credential authenticates.

So a CI pass is a strictly weaker claim than a local `verify_wp_a.sh` pass. The
smoke test prints which mode it ran in; treat "green in CI" and "works" as
different statements, because the gap between them is where this project's
defects have lived.

**Verification note.** Host-backed rows are proven against the *installed*
privileged helper, which `tests/common/mod.rs` enforces by refusing to run
unless `sha256(installed) == sha256(built)` (finding F-11 / TEST-01).

---

## A. Requirement conformance

### Storage volumes (VOL, Section 3.6)

| ID | Requirement | Implementation | Test | Status |
|---|---|---|---|---|
| VOL-01 | Fixed-size presets (500MB/2GB/10GB) | `volume.rs` `VolumeSize` | `size_presets_match_vol_01` | ✅ |
| VOL-02 | Sparse ext4 on a loop device, bind-mounted | `volume.rs` `allocate_sparse_file`/`format_ext4`; helper `loopdev::attach` + `safe::mount_ext4` | `vol_provision_mount_and_ownership_success_path` | ✅ |
| VOL-03 | Every mount/loop/format operation logged | `volume.rs::audit`, helper `audit` | smoke step 1–9 shows the log | ✅ |
| VOL-04 | RAII cleanup on error paths | `Volume: Drop`; `create` failure path | `ac_3_4_fault_injection_leaves_no_orphans` (ignored→host) | 🟡 (integration test still `#[ignore]` in volume.rs; superseded coverage in regression.rs owed) |
| VOL-05 | At-capacity ops fail clearly, no silent loss | ext4 ENOSPC surfaces; `usage()` uses `f_bavail`/`f_bfree` correctly | `ac_3_3_writing_past_capacity_fails_cleanly` (ignored) + smoke step 8 df cross-check | 🟡 (dedicated non-ignored ENOSPC + statvfs unit test owed — F-30) |
| VOL-06 | `start` verifies mount via mountinfo, remounts or fails loudly | `project.rs::ensure_volume_mounted`; `volume.rs::is_mounted` (octal-unescaped) | `vol_06_start_remounts_a_volume_lost_to_reboot`, `vol_06_missing_backing_file_fails_loudly`, `is_mounted_decodes_octal_escaped_mount_points` | ✅ |

### Privilege model (PRIV, Section 3.7)

| ID | Requirement | Implementation | Test | Status |
|---|---|---|---|---|
| PRIV-01 | Rootless containerd; no root-equiv group | `client.rs::default_socket_path` (rootless socket only) | smoke prereqs; runs entirely rootless | ✅ |
| PRIV-02 | losetup/mount privileged; mkfs/alloc not | `volume.rs` (alloc/mkfs unprivileged) + helper (loop/mount) | success-path test | ✅ |
| PRIV-03 | Single root-owned helper, wildcard-free grant, all validation inside | `deploy/nemr-volume/`, `deploy/sudoers.d/nemr-volume`; `safe::open_beneath` fd resolution | `symlinked_*_is_refused` (×3), `sudoers` grant shape | ✅ (was a **local root escalation**, F-01/F-02/F-04; fixed) |
| PRIV-04 | Elevated ops logged as elevated | helper `audit` (`[elevated] …`) | smoke shows `helper:` lines | ✅ |
| PRIV-05 | Split holds Phase 1; widen only via escalation | design; helper spawns zero subprocesses | — | ✅ |
| PRIV-06 | Mount+chown atomic; chown to invoker uid; no separate chown grant | helper `cmd_mount` (`fchown` on the mounted-root fd) | `vol_provision_mount_and_ownership_success_path` (user can write) | ✅ (was chown-follows-symlink escalation, F-05; fixed) |

### Authentication (AUTH, Section 3.5)

| ID | Requirement | Implementation | Test | Status |
|---|---|---|---|---|
| AUTH-01 | Credentials not baked into the image | `image/Dockerfile` (no creds) | smoke step 4 | ✅ |
| AUTH-02 | Host `~/.claude/.credentials.json` bind-mounted read-only | `auth.rs`, `project.rs::create` (`BindMount::read_only`) | smoke step 4 (readable + read-only) | 🔶 (narrowed from whole `~/.claude` to the file, SPEC 1.14) — WP-C1 confirmed it is the **only** secret and stays a host bind-mount off both portable layers (D-02). **F-12** open: a file bind-mount pins an inode, so an atomic-replace credential rotation on the host is invisible in-container. |
| AUTH-03 | Missing credentials → clear, actionable failure | `auth.rs::resolve_credentials` | `missing_credentials_error_is_actionable` | ✅ |

### State locality & relocation (WP-C, new — pending Section 3 promotion)

| ID | Requirement | Implementation | Test | Status |
|---|---|---|---|---|
| STATE-01 (C1) | Determine empirically where session state lives | `docs/state-locality.md` (measured both layers) | experiment (evidence in doc) | ✅ history on rootfs, project files on volume; credential is a host bind-mount |
| M8 (C2) | Relocate session-critical state onto the portable volume | `project.rs::session_state_mounts`, `create`; `config.rs` state paths | `m8_session_state_lives_on_the_volume_and_vanishes_when_unmounted` + real-API `--continue` recall | ✅ surgical (history subtrees only; credential + `.claude.json` identity stay off the volume, D-02) |

### Bundle portability and the storage seam (WP-D — M9–M12)

| ID | Requirement | Implementation | Test | Status |
|---|---|---|---|---|
| M9 | Versioned bundle format: manifest, checksums, zstd, exclusion policy | `bundle/{manifest,export,policy}.rs`; `docs/bundle-format.md` v1 | 34 bundle unit tests; `m9_a_real_bundle_contains_no_credential`, `m9_export_does_not_swallow_bundles_in_the_workspace` | ✅ |
| M10 | Import restores a session on another host | `bundle/import.rs`, `project::import` | `m10_bundle_round_trip_carries_the_session_to_another_project`; **live-API acceptance**: `claude --continue` recalled CARDAMOM-77 on a destination proven empty first | ✅ |
| M11 | Hardening: old versions, base-image drift, corrupt/truncated, digest and quota mismatch | `import.rs` decide→verify→extract | 6 `m11_*` tests via a hostile-bundle fixture, **each proven red with its guarded code disabled**; `m11_a_hostile_bundle_cannot_escape_the_destination` | ✅ |
| M12 | S3-compatible trait, R2 first, B2 viable behind it | `crates/nemr-storage` (**commercial side**, E-11): `ObjectStore`, `local`, `s3`, `conformance` | 16 storage tests; conformance suite run against `LocalStore` **and proven to reject a corrupting backend** | 🟡 **live bucket round-trip outstanding** — needs a credential this session does not hold; run `bucket_roundtrip` (see below) |
| E-11 seam | The open half must not depend on the commercial half | dependency direction; `scripts/check_seam.sh` in CI | proven red when `nemr-engine` depends on `nemr-storage`; carries a control so a crate rename cannot make it pass vacuously | ✅ |
| D-05 | Egress-conscious storage interface | `head` (no transfer) and `get_range` (manifest prefix) on the trait | `get_range_returns_a_prefix_and_clamps_past_the_end`; S3 `get_range` clamps rather than letting the service 416 | ✅ |
| D-02 (M12) | A storage credential must not reach a log | `S3Config` `Debug` redacts both halves; error text truncated and stripped at `?` | `debug_redacts_the_credential_but_keeps_diagnostics` (with a control that the bucket *does* appear) | ✅ |

**M12's remaining gap, stated precisely.** Everything except the live bucket is
tested. The outstanding step is one command, run by whoever holds the credential:

```
NEMR_S3_PROVIDER=r2 NEMR_S3_BUCKET=… NEMR_S3_ENDPOINT=… NEMR_S3_ACCESS_KEY_ID=… NEMR_S3_SECRET_ACCESS_KEY=… cargo run -p nemr-storage --bin bucket_roundtrip -- <bundle.nemr>
```

It uses a PID-unique key under `nemr-roundtrip/`, deletes on every exit path
including assertion failure, and prints no credential. Until it has run, "one
cloud backend works end to end" is **unverified** — the local conformance pass is
evidence the suite is correct, not evidence R2 is.

### Process model (PROC, Section 3.8)

| ID | Requirement | Implementation | Test | Status |
|---|---|---|---|---|
| PROC-01 | PID 1 is a long-lived supervisor | `config.rs::SUPERVISOR_ARGS` | `proc_06_*` | ✅ |
| PROC-02 | `attach` is a task exec with a fresh TTY per call | `project.rs::attach`, `ModeTracker` (`tty.rs`) | smoke step 3–5; `mode_tracker_tests` (11) | 🔶 **E-07**: a pty is allocated only when stdin is a terminal (a scripted attach must be pipe-backed to terminate). Drift from PROC-02 as written; awaiting Section 3.8 amendment. `ModeTracker` now unit-tested (F-40 closed; found+fixed an ESC-restart parser gap). |
| PROC-03 | Supervisor written explicitly into the runtime spec | `project.rs::create` (`args`) | — | ✅ |
| PROC-04 | `stop` terminates the task incl. live execs | `containers.rs::stop_task` (SIGKILL-all) | — | 🟡 (exec-kill path untested — F-35) |
| PROC-06 *(new)* | Supervisor must trap SIGTERM; `stop` terminates gracefully, not by timeout→SIGKILL | `config.rs::SUPERVISOR_ARGS`, `StopOutcome` | `proc_06_stop_terminates_gracefully_without_escalating_to_sigkill`, `proc_06_supervisor_installs_a_sigterm_handler` | ✅ |

### Network (NET, Section 3.9)

| ID | Requirement | Implementation | Test | Status |
|---|---|---|---|---|
| NET-01 | Share rootlesskit's netns (host-net equivalent) | `containers.rs::oci_spec` (no netns entry) | smoke API round-trip | ✅ |
| NET-02 | Per-project net isolation out of scope | — | — | ✅ (documented) |

### Non-functional (NFR, Section 5)

| ID | Requirement | Implementation | Test | Status |
|---|---|---|---|---|
| NFR-01 | No Docker at any layer, incl. transitively | `scripts/check_docker_free.sh` (CLI/socket, transitive deps, build path) + `deny.toml` bans | CI `docker-free` job; **negative-tested** three ways (socket path, CLI invocation, real `bollard` dep) | ✅ (F-24 closed — now enforced by the pipeline, not memory) |
| NFR-02 | create/start in low single-digit seconds | — | — | 🟡 (measured nowhere — F-45, WP-B benchmark harness) |
| NFR-03 | No orphaned loops/mounts/containerd resources after delete | recoverable `delete` ordering + `reconcile_orphans` | live reconcile test; smoke step 9 | ✅ (cluster F-03/F-10/F-15/F-20/F-23 fixed; exec-record leak on SIGKILLed attach still open — F-18) |
| NFR-04 | Destructive ops logged auditably | `audit` via `tracing` (info, on by default); `NEMR_DEBUG=1` adds decision points | smoke output; VOL-05 debug demo in README | ✅ |
| NFR-05 | No silent privilege escalation; documented | helper via explicit sudo; `setup_test_host.sh` | — | ✅ |

### Acceptance criteria (AC)

AC-1.x (connectivity), AC-2.x (image build), AC-3.1–3.5 (volume), AC-4.x
(create), AC-5.x (start/attach/stop), AC-6.x (list/delete), AC-7.x (E2E) — all
demonstrated historically and by the smoke test. Owed: **AC-3.x volume tests are
still `#[ignore]`d in `volume.rs`** (F-09, F-16); **AC-4.2/AC-5.3 error paths
have no automated test** (F-48); **AC-7.1 "clean host" needs the helper install
scripted** — done via `setup_test_host.sh`.

---

## B. Audit findings ledger

41 confirmed by an adversarial audit (16 agents, find→refute), plus findings
raised since. IDs are stable; disposition tracked here.

**Fixed on `wp-a-hardening`**

| ID | Sev | Requirement | Finding | Fixed by |
|---|---|---|---|---|
| F-01 | crit | PRIV-03 | Mount point checked with symlink-following `is_dir()` → mount attacker ext4 over `/etc` = root | fd-based `open_beneath` + in-process mount |
| F-02 | crit | PRIV-03 | Backing file re-opened by name after check (TOCTOU) → attach `/dev/sda1` | pinned fd + `/proc/self/fd` |
| F-05 | high | PRIV-06 | `chown` follows symlinks → chown `/etc` to caller | `fchown` on mounted-root fd |
| F-06 | high | VOL-06 | Concurrent `start` double-attaches/double-mounts one image → corruption | backing-file flock + `--nooverlap` intent |
| F-07 | high | VOL-05/06 | `is_mounted` compares raw path to octal-escaped mountinfo → mounted reads unmounted | octal-unescape (engine + helper) |
| F-03/F-10/F-20 | high | NFR-03 | `delete` destroys record before releasing volume → unrecoverable orphan | reorder: record removed last + idempotent |
| F-15/F-23 | med | NFR-03 | Orphan loop/snapshot after crash, no sweep | `reconcile_orphans` + `nemr reconcile` |
| F-17 | high | PROC-04/06 | `sleep infinity` ignores SIGTERM → every stop 6.4s→SIGKILL, reported success | trapping supervisor + `StopOutcome` |
| F-11 (TEST-01) | — | — | Helper passed 13 tests while unable to provision (rejection-only coverage) | hash gate + success-path test |
| F-19 | med | NFR-03 | No engine↔helper version handshake | `version` subcommand (protocol 2) |
| F-30-loop | med | NFR-03 | Loop lookup by path string breaks on `(deleted)`/whitespace | inode-identity via `LOOP_GET_STATUS64` |
| F-30 | med | VOL-05 | statvfs `f_bfree`/`f_bavail` + `Usage::percent` untested | 3 `usage_percent_*` unit tests (df definition) |
| F-40 | med | PROC-02 | `ModeTracker` untested; ESC-restart parser gap | 11 `mode_tracker_tests` + ESC-restart fix |
| F-09/F-16 | high | VOL-06/AC-3 | AC-3.x + VOL-06 tests `#[ignore]`d in volume.rs | migrated to the non-skippable suite; zero ignored tests remain |
| F-24 | med | NFR-01 | No CI — Docker-freeness/lint/licensing unenforced | `.github/workflows/ci.yml` + `check_docker_free.sh` (negative-tested) + `deny.toml` |
| F-37 | med | — | base-image build never scripted (README prose only) | `scripts/build_base_image.sh`, run and verified (200.5 MiB, same digest) |
| F-53 | high | NFR-01/supply chain | **RUSTSEC-2026-0258** — `h2` unbounded empty DATA frames, reachable via `tonic` → `containerd-client` | found by the new `cargo deny` gate on its first run; `h2` 0.4.15 → 0.4.18 |
| **F-54** | high | AUTH-02 / D-02 | `.claude.json` mixes portable config (MCP servers) with machine/account identity (`machineID`, `oauthAccount`) — a file-level include/exclude either leaks identity or drops MCP config | **Ruled 2026-08-21:** field-level **allowlist**; unrecognised fields stay and are logged. Implemented in M9's exclusion policy. |

**Open — this branch / next (WP-A remnant + WP-C follow-up)**

| ID | Sev | Requirement | Finding | Plan |
|---|---|---|---|---|
| F-18 | med | NFR-03 | SIGKILLed attach leaks its containerd exec record | reap stale execs in `reconcile_orphans` |
| E-11 | — | open-core seam | `nemr export`/`import` must work with no network and no credentials, against a local file | `e11_export_and_import_work_with_no_network_and_no_credentials` — runs the CLI in a loopback-only netns with a tmpfs over `~/.claude`; proven red when export takes a network dependency and when it requires a credential | ✅ enforced, no longer only by construction |
| **F-60** | high | harness | **A green suite that ran nothing.** `cargo test` exits 0 having run zero tests, so a mistyped name filter prints `running 0 tests / test result: ok` and passes. Worse, a host-backed test that returns early under `NEMR_TEST_UNIT_ONLY` is counted as **passed** — CI's unit job reported *16 passed while 14 did nothing*, on every PR. Green-over-nothing in the harness rather than the code. | **Fixed:** `verify_wp_a.sh` and the CI host job assert every existing test ran and none skipped (proven to reject both a skipped run and a name-filtered partial run); the CI unit job now prints and asserts its real coverage, so its green means "host-free tests pass", never "the suite passes". |
| **F-63** | **high** | VOL/PROC | **`nemr create` could report success having built a container that would never start.** `create_container` prepared the rootfs snapshot and only afterwards wrote the record referencing it. In between, the snapshot had no lease and no referrer — containerd's definition of garbage — and `create_container_record` still returned `Ok`, because containerd does not validate that `snapshot_key` resolves. Failure surfaced later at `nemr start`: `snapshot "nemr-<project>" does not exist`. | **Fixed.** A lease is acquired before `prepare` and released only after the record write, on both paths, with `containerd.io/gc.expire` bounding a crashed process's leak. `prepare_snapshot` takes an explicit lease argument so no call site inherits the unsafe default. Guarded by `f63_create_container_holds_its_snapshot_against_a_collection_inside_the_window`, **proven red 3/3 with the lease removed and green 3/3 with it**. Two supporting tests: the leased-vs-unleased A/B (with the unleased snapshot as the control proving the collector ran) and a no-leaked-lease assertion. `gc_probe` retained as runnable evidence. |
| **F-63b** | med | harness | **The first F-63 guard passed with the defect restored.** The initial regression test raced the collector under concurrent churn and went green twice with the lease removed. Measured properly, the unfixed path loses its snapshot in **1 create out of 80** (~1.25%), so the test would have missed the defect 98.75% of the time while reporting green over it — the exact F-56 shape, committed by the fix's own guard. | **Fixed by deletion, not tuning:** replaced with a deterministic version using a documented test seam that fires a synchronous collection inside the window. Runs in ~1s instead of 70s and is red every time. Recorded because "the guard I wrote for a high-severity fix was itself vacuous" is worth a finding ID, not a footnote. |
| **F-68** | low | harness | **A wall-clock assertion that failed on a healthy engine.** `proc_06_stop_terminates_gracefully` asserted `elapsed < 3s` and fired at 3.47s on a machine that happened to be compiling. The property it exists for — the supervisor exits on SIGTERM rather than being SIGKILLed — is already fully asserted one line above by `StopOutcome::Graceful`; the duration check was a load-sensitive proxy for a claim it did not need to make. It also carried its own `Duration::from_secs(5)` literal for the grace period, so it could silently diverge from the engine's actual value. | **Fixed:** the threshold is now expressed relative to the engine's real grace period (`config::SIGTERM_GRACE`, now public and referenced rather than copied), and set close enough to it that it fires only for "handled, but nearly escalated" — the one case `StopOutcome` cannot express. Three seconds expressed "the machine is busy", which is not a defect in anything under test. |
| **F-69** | med | PROC | **The SIGKILL escalation path had never run.** `proc_06_stop_terminates_gracefully_without_escalating_to_sigkill` asserts escalation does *not* happen for a well-behaved container. Nothing asserted it *does* happen when it should: `StopOutcome::Killed` was never produced anywhere in the tree, by any test or any other path, and neither was the user-facing "had to be killed" message in `nemr.rs`. Had the timeout branch in `stop_task` been dead, every existing test would still have passed — a container ignoring SIGTERM would hang forever with no guard noticing. | **Fixed:** `proc_06_a_container_ignoring_sigterm_is_escalated_to_sigkill` runs a PID 1 with `trap '' TERM` so escalation is the only way it can end, and asserts both that the outcome is `Killed` and that the grace period was honoured first — escalating immediately would pass a naive check while defeating graceful stop for every well-behaved container. Proven red by making `stop_task` report `Graceful` unconditionally. |
| **F-70** | med | D-07 | **Three error variants were unreachable while the engine reported those conditions as untyped strings.** `Error::ProjectExists`, `Error::InvalidName` and `Error::CapacityExceeded` appeared only in `kind()`'s match arms — never constructed, so their `Display` had never been rendered. Meanwhile `create` bailed with `anyhow!("project already exists")` and `validate_name` returned `anyhow::Result`. D-07 deferred gRPC status mapping to the daemon **on the premise that the taxonomy exists now**; under that mapping each of these became `Internal` — wrong for what are plainly caller errors. | **Fixed (ruled: do it).** `validate_name` returns `Error::InvalidName`; `create`'s two duplicate-name checks return `Error::ProjectExists`. `CapacityExceeded` was **deleted rather than wired to an invented call site**: it duplicated `QuotaMismatch`, which is produced by the import capacity check, and two variants for one condition — one unreachable — is the taxonomy half-applied, not a richer one. `ErrorKind::CapacityExceeded` remains as `QuotaMismatch`'s class, so what a daemon switches on is unchanged. Guarded by `the_caller_error_variants_render_their_subject`, which renders each message and asserts its `kind()`; proven red by reclassifying `ProjectExists` as `Internal`. The test caught my own error first — I had asserted all three were `InvalidRequest`, and `ProjectExists` is a `Conflict`, which is the difference between a 409 and a 400. |
| **F-71** | med | harness | **The suite's serial requirement was documented and unenforced.** `tests/regression.rs` has always stated that `--test-threads=1` is *required* — these tests attach loop devices, mount filesystems, start containers and drive containerd's collector, all global host state. Nothing checked it, so `cargo test --test regression` ran them across `nproc` threads and produced failures belonging to the harness rather than the engine. This cost real time: F-63 was first seen in a parallel run, and "you ran it unsupported" stayed a live explanation until a serial arm ruled it out. Same class as F-60 and F-64 — a rule that exists and is never applied. | **Fixed:** `require_serial_execution()` refuses unless `--test-threads=1` or `RUST_TEST_THREADS=1` is present, reading the test binary's own argv rather than inferring. Placed **after** the `NEMR_TEST_UNIT_ONLY` return, since unit-only runs touch no host state and CI's unit job is right to omit the flag — verified against all three invocations. `NEMR_TEST_ALLOW_PARALLEL=1` keeps the deliberate concurrency experiments possible and prints that a failure in that mode is not a defect unless it reproduces serially. |
| **F-72** | med | harness | **The F-63 A/B guard raced the collector it was reasoning about, and could not say which way it failed.** The test created one leased snapshot and one unleased one, checked both existed, then collected. But an unleased snapshot is unreferenced *from the instant it is created*, so containerd may collect it at any moment — including before the baseline check. It failed in **both directions**: locally in a full serial run the unleased snapshot *survived* (nothing had crossed the mutation threshold), and on CI a snapshot was *already gone* at the baseline (23 preceding tests had). Worse, the assertion was `contains(leased) && contains(unleased)` with a message naming neither, so "a snapshot is missing" was ambiguous between *the harness raced* and *the lease is not protecting* — opposite conclusions from identical output. | **Fixed:** both snapshots now start **under leases**, making the baseline deterministic, and the control is made collectable by releasing its lease at a moment the test chooses. Every transition is test-controlled. Demonstrated decisively: with a collection injected between prepare and the baseline, the old shape fails exactly as CI did and the new shape passes. Assertions now name the snapshot and state which conclusion follows. Proven red two ways — lease ignored (kept snapshot collected) and collection disabled (control survived). Note on the CI run itself: which snapshot was missing cannot be recovered from that log, but the two sibling lease tests passed in the same run, so the leased one was almost certainly fine; the new messages remove the need for that inference. |
| **F-73** | med | harness | **The smoke test discarded the API error and reported a single unhelpful line.** `claude -p ... | grep -c NEMR_E2E_OK || true` collapsed every outcome to a count, so no credential, an expired credential, no network egress and a missing CLI all produced `FAIL Claude Code API round-trip succeeded`. Running the command by hand said `Failed to authenticate: OAuth session expired and could not be refreshed` immediately. **Third instance** of this class after F-65 (diagnostics unreachable under `bash -e`) and F-67 (tracing discarded in tests). | **Fixed:** the output is captured and printed on failure, with the likely cause named only where the text makes it unambiguous — and an explicit "cause not recognised, read the output" branch otherwise, because an error that invents a cause is worse than one that hands over the text. The expired-credential branch also points at F-12, since that is the trap immediately behind it. |
| **F-74** | **high** | NFR-01 | **The base image was not reproducible, and no CI check ever verified that it was.** Two hosts building the same Dockerfile produced `sha256:2c4127a5…` and `sha256:4074f744…` with Claude Code 2.1.227 and 2.1.240. **Correcting the premise: there is no reproducible-base-image check in CI and there never was** — five jobs, none builds twice or compares digests. Nothing was passing while broken; nothing was checking. Measured causes, in order of discovery: (1) `FROM node:22-slim` is a moving tag; (2) `npm install -g @anthropic-ai/claude-code` had no version; (3) **even fully pinned, two cold builds on one host minutes apart still differed** — file mtimes, and files that embed the build time in their *contents* (`/var/log/apt/*`, `/var/log/dpkg.log`, `/var/log/alternatives.log`, `/var/cache/ldconfig/aux-cache`, `/root/.npm/_logs`, `/tmp/node-compile-cache`). | **Fixed, and now genuinely reproducible.** `FROM` pinned by digest, Claude Code pinned behind a build arg, `SOURCE_DATE_EPOCH` + `rewrite-timestamp=true` normalising mtimes, and the residue removed in the layer that creates it. **Three cold builds produced one digest (`sha256:2be53736…`); two builds without the epoch flag produced two different digests** — both halves proven load-bearing. `scripts/check_base_image_reproducible.sh` builds twice and compares, in CI, proven red by restoring a single `rm -rf` line. `scripts/check_base_image_pins.sh` guards the pins and the flags, proven red four ways including a control. **Cross-host, demonstrated:** a clean GitHub Ubuntu runner built `sha256:2be53736…` — the same digest as the developer host, on a different machine with a different provenance. That is the exact failure this finding started from, now not reproducible. **Limit stated rather than implied:** `apt-get update` is still unpinned, so this is determinism at a point in time, not immutability — builds weeks apart may legitimately differ, and distribution by digest (D-08/D-10) remains the only thing that makes two hosts agree indefinitely. |
| **F-75** | med | setup | **`/usr/local/libexec` does not exist on a clean Ubuntu.** `setup_test_host.sh` used `install` without `-D`, so every first-time helper install failed with `cannot create regular file … No such file or directory`. The reference host had the directory from accumulated state, which is why it survived until a genuinely clean machine was provisioned. | **Fixed:** `install -D` for both the helper and the sudoers file. Proven: `install -D` creates the parent, and the control without `-D` reproduces the exact error. |
| **F-76** | low | setup | **The sudoers file's counter-example read as a live grant.** A commented illustration of the *rejected* formulation hardcoded `/home/nemr/...`, which on a host whose account is not `nemr` looks like a stale real path. | **Fixed:** rewritten with `<user>` and `<home>` placeholders, and labelled as a rejected formulation. |
| **F-77** | **high** | VOL-05 | **A deleted volume image stays on disk, and every cleanup path reported success while freeing nothing.** Three compounding defects, all found by measuring a real host that had filled its disk. (1) The helper located the loop device by the backing file's *inode*, so once the image was unlinked it unmounted, logged "nothing to detach by inode", and left the device attached — the kernel keeps the unlinked inode alive, so a fully-allocated 500 MB image occupies space no `rm` can reclaim. Seven were stranded this way, ~3.5 GB. The code comment asserting "a detached-and-deleted device auto-clears once its mount is released" is **false, measured**: the mount was released and the device was still attached. (2) `reconcile` gated release on `is_mounted`, skipping exactly the unmounted-but-attached state that holds the disk. (3) `reconcile` pushed to `released` whenever the helper returned `Ok` and printed *"released orphan volume … (mount + loop device)"* — nine reported released, **zero bytes reclaimed, zero devices detached**. Success asserted rather than observed, in the cleanup command. | **Fixed.** The helper now recovers the device from mountinfo field 3 (`major:minor`, major 7 = loop) captured *before* unmounting, with a `/sys/block/loop*/loop/backing_file` fallback for a volume already unmounted — both anchored to the project's own image path, so neither can select an unrelated device. `reconcile` no longer gates on `is_mounted` and **reports what the host shows afterwards**, naming the stranded device per volume. `list` surfaces untracked volumes including loop-only residue (scanning the image directory alone missed all seven). The test harness sweeps volumes left by a killed run — `TestProject::drop` never runs under SIGKILL — filtered to `<prefix>-<pid>-<n>` names whose pid is dead, proven red both by removing the liveness check and by accepting any name. Parser guards proven red two ways. **Not yet verifiable end to end:** the helper must be reinstalled by root before the detach can be exercised on this host. |
| **F-63a** | med | harness | **Did not reproduce in a clean measurement.** Originally: `m8` failing at restart with `.nemr-state/projects: no such file or directory`, plus one later run where the history file came back *empty* with the remount logged successful. Re-measured after F-71 removed the parallelism confound and F-77 stopped volumes accumulating: **20 serial runs, 0 m8 failures**; the single red in that arm was an unrelated test (F-78). Earlier counts, for comparison: 3 of 38 loaded-parallel pre-F-63-fix, 1 of 10 post-fix but confounded. | **Open, not reproduced.** Deliberately not closed: 20 runs is not proof of absence for something originally seen at roughly 1-in-13, and the two observables were never shown to share a cause. What changed is that a recurrence is now self-diagnosing — the content assertion reads the file directly from the volume at failure time and prints size, content and mount state, so the next occurrence says whether the *data* was lost or the *exec capture* was. Proven to discriminate by injecting an empty capture: it reported `file size: Ok(20)` and the correct content, i.e. "the data is fine". |
| **F-78** | med | PROC-06 | **`stop` failed once in 20 runs with "did not exit within 10s of SIGKILL".** SIGKILL is uncatchable, so a task surviving it is not running — it is in an uninterruptible wait. | **DIAGNOSED (not a bug in nemr; do NOT lengthen the timeout).** A bounded harness (`crates/nemr-containerd/src/bin/f78_probe.rs`, capped at 50 runs / 2h, disposable containers, per-iteration sweep, output flushed so the run that matters cannot be swallowed) reproduced it and captured the mechanism from `/proc` directly. **The kill target is genuinely blocked, not lost by the supervisor:** the `proc_06` payload `sh` running `while :; do sleep 1; done` was caught in **State: D (uninterruptible), `wchan: kernel_clone`** during the kill window — i.e. mid-`fork()` of its next `sleep` child. A task inside `kernel_clone` cannot receive SIGKILL until the clone returns. **Rate is payload- and load-dependent:** clean (unloaded) 50/50 runs succeeded with **zero** kill-window D-states and max latency 1620 ms — every D seen was at *startup* (runc exec), never at kill; under CPU oversubscription (10 spinners / 6 cores) it reproduced on the first iteration, the fork's D-window stretched past 10 s. The fork-heavy test payload (a `clone()` in flight almost every second) is unusually prone; a long-lived agent forks rarely. **Consequences recorded, no fix this pass:** (1) the 10 s timeout is the right shape — it bounds the wait and returns an error rather than hanging forever; lengthening it only delays the same hang. (2) Under CPU starvation the runtime's own timer fires late, so "10 s" is not wall-clock-guaranteed — the daemon (E-09) inherits this and should classify "wedged in uninterruptible sleep" as a distinct, surfaced state rather than a generic error or a silent success. (3) The harness is kept as a committed diagnostic, like `gc_probe`. |
| **F-79** | **high** | VOL-05 | **`list` and `reconcile` disagreed about the same host, and the cleanup command gave the false all-clear.** `nemr list` reported 57 untracked volumes holding 24 GB; `nemr reconcile` answered *"nothing to reconcile: no orphaned mounts, loop devices or snapshots"*. Both read the host, but different parts of it: `list` enumerates loop devices from `/sys`, while `reconcile` enumerated mount-point *directories* and image *files* — and those 57 had neither, their images unlinked and their mount points removed. **Self-inflicted, in the same pass as F-77:** the test-harness sweep deleted the image and mount point on the strength of `unmount_and_detach` returning `Ok`, so when the old helper silently failed to detach (F-77) the sweep destroyed the only two things `reconcile` enumerates. The cleanup hid the mess it had failed to clean. | **Fixed, verified end to end on the affected host: 14 GB free → 38 GB, 58 loop devices → 1** (the one real mounted project). `reconcile` now enumerates stranded devices from `/sys`, the same source `list` uses, so the two share an enumeration rather than each guessing from a different one. The harness sweep verifies the detach actually happened before removing anything, and leaves the artifacts in place when it did not, so `reconcile` can still find them. Guarded by `f79_reconcile_acts_on_everything_list_reports_as_untracked`, asserting the structural property — everything `list` calls untracked must appear in `reconcile`'s account — with a control that nothing remains untracked afterwards. **Red evidence is the production incident itself** (list=57, reconcile=0, observed live before the fix) rather than a synthetic one: with the fixed helper the stranded state can no longer be fabricated. |
| **F-80** | med | NFR-01 | **The Docker-freeness control survived the base-image rename by accident.** It asserted that a `docker.io/` reference existed under `src/` — true while the base image was named `docker.io/nemr/base`, and its purpose was to prove the checker correctly does *not* flag legitimate OCI registry naming. After the rename to `ghcr.io`, the only remaining matches were **doc comments about the rename**, so the control still passed while testing nothing. A control that survives by accident is worse than one that fails: it reports confidence it has not earned, and the next person to tidy a comment would have broken it for a reason unrelated to what it guards. | **Fixed:** the control is now self-contained — it feeds the checker's own pattern a known registry reference and a known violation in a temporary directory, and asserts the first is *not* flagged while the second *is*. It holds whatever the tree happens to contain. Proven red both ways: broadening the pattern so the registry reference is flagged, and narrowing it so `docker run` is not. |
| **F-81** | ~~high~~ **RESOLVED** | D-08/E-11 | **The published base image was not pullable without an account** — GHCR packages pushed with `GITHUB_TOKEN` default to private, and a private package looks identical to success on the publishing side (push ok, digest recorded, nothing reports the artifact is unreachable). | **RESOLVED (2026-08-24, Product Owner made the package public).** Verified anonymously: `gnrain/nemr-base:0.1.0` returns **HTTP 200** (control: `actions/actions-runner` 200). So the D-08 remedy is reachable on another machine today for a published version. `scripts/check_base_image_published.sh` — the check that surfaced this — passes for 0.1.0. **Ledger correction:** this stayed marked open one pass too long; confirming it required a live probe, not memory, which is the standing lesson about the one thing that gates the remedy. The remaining gap is F-86, a different bug (the current version 0.2.0 is not yet published because the publish workflow blocked itself). |
| **F-83** | **high** | PRIV | **A project's disk is mounted without `nosuid` or `nodev`, so it is a local privilege-escalation vector.** The privileged helper mounted every volume with `libc::mount(..., 0, NULL)` — flags literally zero. The backing image is allocated and formatted by the *unprivileged* engine, owned by the invoking user, and the helper checks only that it is a regular file they own — never that its contents are safe. **Proven, safely and end to end short of the escalation itself:** as the unprivileged user, an ext4 image was crafted containing a file with mode `04755` owned by `root:root`, using `debugfs` to set the inode's uid and mode directly on the image file — no root, no mount, no execution. Combined with a `flags=0` mount, executing that file yields root. The kernel confirms the exposure: mountinfo reported the volume as `rw,relatime` with neither `nosuid` nor `nodev`. Found during the README rewrite's fact-check of the claim that the helper "can do nothing else" — it can. | **Fixed in source; needs the helper reinstalled to arm.** `mount_ext4` now passes `MS_NOSUID | MS_NODEV`. Exec is deliberately kept — the mount is the container's `/workspace` and running project code is the point; setuid binaries and device nodes in a workspace are attack surface, not a feature. Guarded by `f83_project_volumes_are_mounted_nosuid_and_nodev`, which asserts the kernel's own mountinfo report (authoritative) with a control that the volume is actually mounted. **Proven red against the currently installed helper.** Green requires `sudo ./scripts/setup_test_host.sh` to reinstall — until then the hash gate is red and the host suite refuses, correctly. The README's helper description was already corrected to not claim more than the code delivers. |
| **F-84** | **high** | E-15 | **Codex is implemented but its session portability is unverified.** Codex runs inside a container (proven: `codex-cli 0.149.0` launches), and every agent-agnostic part is done — the agent field, the manifest's producing-agent, the image, the switch command. But the agent-SPECIFIC portability work is not, and cannot be faked: **(a) state locality** — where Codex writes transcripts, session metadata and config, never measured, assumed to resemble Claude Code's layout, so the M8 relocation bind-mounts may point at the wrong paths entirely; **(b) the M8 relocation** — whether session-critical state actually lands on the portable volume; **(c) credential enumeration (2d)** — every path holding a Codex credential, unenumerated, so **D-02 is asserted for Codex, not enforced** (for Claude Code WP-C enumerated them, which is what makes D-02 enforceable there); **(d) the M8 acceptance** — stop/restart, unmount, prove state is genuinely gone not cached; **(e) the M10 acceptance** — export, import fresh, continue with history intact. | **Open — needs Codex auth on a host, which is unavailable (regional purchase barrier).** Running the WP-C capture without API round-trips would observe an empty directory a real session would have filled — the WP-C failure mode inverted, how a "verified" feature ships having never held a conversation — so it is deliberately NOT attempted. Flagged where it will be read: the CLI warns at selection (menu + create + switch), and `Agent::portability_verified()` carries the fact as data. Closable in an afternoon by anyone with working Codex access, running the WP-C method as written. Gemini (free tier, no purchase) is recorded as the better target for a *verified* second agent when multi-agent work resumes. |
| **F-85** | med | D-08 | **Changing the base image under a fixed version orphaned existing bundles, and the D-08 error's pull advice could not fix it.** Adding Codex changed `ghcr.io/gnrain/nemr-base:0.1.0` from `2be53736…` to `39c5ade9…` under the same `0.1.0` tag; a bundle recording the old digest no longer resolved, and the pull advice named the engine's current version, fetching different bytes — a success signal over the wrong artifact, F-81's shape one layer up. | **RESOLVED (Product Owner ruling).** The image is versioned: `0.2.0` is the two-agent image, `0.1.0` stays published. A version tag names one set of bytes forever — recorded per version under `image/digests/<version>` (immutable files), enforced by the build (refuses a digest disagreeing with its version's record — catches a Dockerfile change with no bump), the publish workflow (refuses different bytes under an existing version), and `check_base_image_versioning.sh` in CI (proven red three ways). The D-08 pull advice now names the version the bundle needs, from the bundle's own reference — verified end to end on a disposable project: a 0.1.0 bundle on a 0.2.0 host is told to pull 0.1.0. GHCR has no confirmed per-tag immutability toggle, so the repo-side checks are the enforcement. |
| **F-86** | **high** | D-08/E-11 | **The publish workflow blocked its own first publish of any new version, and its check conflated "not published" with "private".** Two bugs found while confirming F-81. (1) **Ordering:** the "obtainable without an account" step ran BEFORE publish. A new version is HTTP 404 until pushed, so the check failed on it and the publish step was skipped — a new version could never publish. Confirmed on the `0.2.0` publish run: step 6 (verify) failed, step 7 (publish) skipped. This is why `0.2.0` (the current base image) is 404 on GHCR while `0.1.0` is 200. (2) **Diagnostic:** `check_base_image_published.sh` reported any non-200 as "private — change visibility", sending someone to toggle a setting when the real cause was a 404 (never published). | **Fixed.** The pullability verification now runs AFTER publish (it verifies what was pushed rather than gating on it). The check distinguishes 403 (PRIVATE — visibility toggle) from 404 (NOT PUBLISHED — run the publisher) from other statuses (inspect by hand), each with the right remedy; a `NEMR_TEST_FORCE_STATUS` seam proves all three branches while the known-public control stays network-real so a forced run cannot pass vacuously. The real proof of the ordering fix is the next publish-on-merge actually publishing `0.2.0`. |
| **F-87** | med | F-85/E-11 | **`check_base_image_published.sh` verified only the current version, so F-85's core guarantee — old versions stay reachable at the same bytes so old bundles import — was asserted, not enforced.** It read `NEMR_BASE_TAG` (default 0.2.0) and checked that one tag; `NEMR_BASE_VERSION=0.1.0` was silently ignored, and nothing confirmed 0.1.0 remained pullable or unchanged. An old version quietly going private, 404, or drifting to different bytes would break every bundle built against it with nothing reporting it. | **Fixed.** The check now iterates **every** file in `image/digests/`, and for each version verifies (a) it is anonymously reachable and (b) the registry serves the **exact digest** recorded — a drift (tag moved to different content) fails loudly as the F-85 violation it is, naming the version and that its bundles are now unrecoverable. A new version is covered automatically the moment its digest file is added. `NEMR_BASE_VERSION` now works as a single-version filter (the var the reporter reached for), defaulting to all. Proven red four ways: digest drift, 404, 403, and an empty selection (a filter matching nothing FAILs rather than passing vacuously). The known-public control still runs first, so a real per-version problem is distinguishable from "anonymous GHCR access is down here." |
| **F-88** | **high** | E-09 | **A shared-daemon attach could pin a worker thread forever (DoS), and the audit registry leaked an entry per command.** Found by an adversarial review of the audit-stream implementation. (1) The attach stdout pump used `blocking_send` on a bounded channel; a client that stops reading fills the channel, the pump parks, never observes the teardown `stop` flag, and the async task that `join()`s it pins a tokio worker permanently — repeatable across sessions to exhaust the pool and stall the daemon for all clients. (2) The `WatchAudit` forwarding task removed its registry entry only after `raw_rx.recv()` returned `None`, but the only sender lived in that same registry entry — a circular dependency, so on every normal command the task parked forever holding the entry: an unbounded leak over the daemon's lifetime. (3) The per-command request id was `pid-counter` with a per-process counter resetting to 0, so a reused PID could collide `pid-0` and one client's stream teardown could unregister another's. | **Fixed.** The pump honours `stop` even when the channel is full (`try_send` + stop-check), and the joins run off-runtime via `spawn_blocking`. The forwarding task `select!`s on `out_tx.closed()` so it detects the client disconnect that ends every command and removes its entry. The request id gains nanosecond entropy so ids are unique across processes, not just within one. The review also confirmed as correct (no change): the tower clone-swap, the span enclosing engine events, the `%id` Display capture, and mutex-poison handling. |
| **F-90** | med | harness | **The installed-engine gate cried wolf, and was blind where it mattered.** Raised by the Product Owner after closure: the mtime scan swept `crates/` wholesale, so a sync-server test file — which cannot affect the engine and which the engine must never depend on — reported the engine stale. A gate that false-positives teaches people to reinstall past it without reading, and then it stops working on the day the drift is real. Investigating found the inverse holes too: `build.rs` and `proto/` are compiled into the binary and were never scanned (a proto edit left the gate green), and `nemrd` — installed beside `nemr`, and the process every command actually runs through — was never hash-verified at all. | **Fixed.** Scan scoped to the engine's dependency closure (`src/`, `build.rs`, `proto/`, `crates/nemr-containerd/`, `Cargo.toml`; `Cargo.lock` excluded as workspace-wide, residual named), citing the helper gate's identical lesson from the other direction. `nemrd` now hash-gated alongside `nemr`. Proven: a commercial-crate edit no longer trips it; a `nemr-containerd` edit and a proto edit still do; a corrupted installed `nemrd` is caught. The helper gate was audited for the same defect and is already correctly scoped (its own workspace only, deliberately). |
| **F-114** | note | harness / process | **Residue on the developer host, and what made it.** Investigating F-112 catalogued what was actually on the machine: three `wf-*` projects (2026-08-28 20:51–20:55) created by **workflow subagents this session launched** and never deleted — they were holding session-network indices 1 and 2, which is why the failing project was allocated index 3; one `impro-5671` from my own minimal repro; a stranded `wf-purgeleak` mount directory with no image and no container; an `enospc-…` volume from a killed regression run; and two label-less `nemr-proc06kill-*` container records from 2026-08-22, which are invisible to `nemr list` and to `allocated_indices` (both key on `nemr.project`) and therefore inert but permanent. **None of it caused F-112** — it only chose which `/24` the failure named. | **Mine removed** (the three `wf-*`, `impro-5671`, the stranded mount dir); the rest left alone and reported, because deleting someone's projects is not a cleanup decision to take unilaterally. Two things worth keeping: a mount directory with no image and no container is invisible to the untracked-volume report, which only looks for `.img` files; and giving subagents create/delete on a live host without enforcing cleanup is a process defect, not an engine one — the engine is right not to reclaim projects automatically. |
| **F-113** | med | NET-02 | **The session wiring did not reconcile, so it worked from clean and failed on the second application.** `ip addr add` and `ip route add` answer "RTNETLINK answers: File exists" rather than "already so", so any second application was a hard failure — and because `start` turns a wiring failure into a refusal to start (correctly), that is a project that will not start. This is the pattern this project keeps hitting; volumes and port forwards already work the other way, where the declaration is authoritative and the live state is brought to match it. | **Fixed:** `ip addr replace` and `ip route replace` throughout, a stale `ceth0` inside the session removed rather than collided with, and the link pair already rebuilt from scratch. Pinned by `net02_wiring_a_live_session_again_reconciles_rather_than_colliding`, which wires a LIVE, already-wired session twice more and asserts it stays wired — with a control that asserts it was wired before the re-application, so the test cannot pass by reconciling nothing. |
| **F-112** | **high** | NET-02 / NET-04 | **The NET-02 migration broke every project that predated NET-02 — a regression I shipped in the fix for F-103.** Reported by the Product Owner: `nemr start htmltest` — a project weeks old that had moved between two machines — failed with *"wiring session network 10.99.3.0/24 failed: RTNETLINK answers: File exists"*. **The premise of F-103 was factually wrong.** It said an old project's task "now receives its own empty network namespace whether or not anything wires it". It does not: **a container's OCI spec is frozen into its record at create time and read again at every task start**, so a record created before NET-02 asks for no network namespace for ever, and its task joins rootlesskit's exactly as it always did. F-103 gave such a project an allocation label and then wired it — into the SHARED namespace, where `ip route add default` collides with rootlesskit's own default route. Before F-103 the project had no label, wiring was skipped, and it started fine. Established, not guessed: rootlesskit's namespace was **clean** (no `nemr*` links, no addresses, no NAT rules), so the reported residue hypothesis was falsified; `nemr-htmltest`'s recorded namespaces are `[pid, ipc, uts, mount]` against `[…, network]` for a container created after NET-02; the task's `net:` inode was captured live and equals rootlesskit's; and `ip route add default` into a namespace that already has one was reproduced in a throwaway namespace, giving that exact string and exit 2. | **Fixed:** `ensure_own_network_namespace` adds the namespace to the stored spec **before** the task is created — additive, one entry, every other byte of the spec untouched, because these records carry mounts, a cgroup path and process arguments for projects that have travelled between machines. The migration is announced to the user, once. A **guard** now refuses to wire a task that shares rootlesskit's namespace, so the bad shape is a legible refusal rather than session addresses written onto rootlesskit's interfaces. **A test-control mistake caught on the way:** the first version of the upgrade test established its precondition by calling `ensure_own_network_namespace`, which MIGRATED the subject — so the code under test received an already-repaired container and the test stayed green with the migration deleted. A control that changes what it observes is not a control; it now reads with `has_own_network_namespace`, and the mutation goes red. Verified on the real thing: `wphcreate` (disposable, same shape) first, then `htmltest` — migrated, own namespace, wired at 10.99.3.2/24, DNS resolving inside it, workspace intact, across three start/stop cycles, with rootlesskit's namespace back to `lo`+`tap0` after each. |
| **F-111** | med | E-09 | **Three ways the daemon's socket handling made failures unattributable, all reproduced.** (1) `nemrd` unlinked the socket path BEFORE connecting to containerd, justified in a comment by "the connect check below would have found it" — that check lives in the CLI, in a different process. A daemon that could not reach containerd therefore deleted a *working* daemon's socket and exited, leaving every CLI on the machine with nothing to talk to; and on the happy path the path was absent for the whole containerd connect (measured 36ms idle, over a second under load), during which every CLI got ENOENT and autostarted another daemon, which unlinked and re-opened the window in turn. **Six live `nemrd` processes were observed on one machine**, five of them orphaned — which contradicts E-09's single-writer claim exactly where it matters. (2) The socket was created by `bind` at `0777 & ~umask` and only then chmod'ed to `0600`, so there was a window in which any local user could reach the daemon this file's own header calls the trust boundary. (3) `handshake()` had no timeout, unlike the 5s connect and the 15s audit-Ready wait around it: `nemr list` hung 61 seconds against a socket that accepted and completed the HTTP/2 handshake but never answered, and was still hung when killed — in CI, a step that burns its timeout and says nothing. | **All three fixed.** containerd is connected first; the daemon then binds beside the real path, narrows permissions, and `rename(2)`s into place, so the well-known path is never absent and never briefly world-reachable. A daemon that finds one already answering exits rather than displacing it. Shutdown removes the socket only if its dev/inode still match the one it bound, so it cannot take out a successor on the way past. The handshake is bounded at 15s with a message that says what to do. The autostart message no longer says "within 5s" while waiting 15 — it computes the window from the loop. |
| **F-110** | **high** | UX / F-109 | **`nemr` panicked whenever its reader went away.** Rust sets `SIGPIPE` to `SIG_IGN` before `main`, so a write to a closed pipe returns `EPIPE` and `println!` panics. Measured: `nemr list | head -1` printed `thread 'main' panicked ... failed printing to stdout: Broken pipe` and exited 101 in **19 of 100 runs**. Every `nemr <cmd> | head`, `| grep -q`, `| less`-and-quit a user types was a coin flip on a stack trace — and it is the same event that produced F-109's false failure, one layer down. | **Fixed:** `SIGPIPE` is restored to `SIG_DFL` at the top of `main`, so the process dies quietly like `cat` or `ls`. Pinned by a regression test that runs the INSTALLED binary into a closing reader 40 times and fails on any panic, with a control that fails if the probe never ran the binary — a race needs repetition, and one green run proves nothing. Measured after the fix, 200 runs under six CPU hogs: **0 panics**, and 19 clean SIGPIPE deaths (status 141). That second number is the point — **the SIGPIPE fix does NOT remove the false failure.** Under `pipefail` a writer that dies of SIGPIPE still fails the pipeline, so `cmd \| grep -q` would go on reporting a match as a miss 9.5% of the time under load. Only not using a pipe removes it, which is what `output_has` does and why F-109 is fixed there rather than here. |
| **F-109** | **high** | harness / WP-K | **An acceptance assertion that could not tell three different things apart — and the cause of the failure it reported is still not established.** `scripts/sync_acceptance.sh` step 7 fired with *"pulled project not in nemr list"* in CI, having just printed `nemr pull`'s complete success. The assertion was `nemr list 2>/dev/null | grep -q "^$PROJECT " || fail ...`, which answers "no" three ways: the project is absent, the listing FAILED (stderr discarded, and under `set -o pipefail` a failed listing produces exactly this message), or `grep -q` matched and exited first, giving the writer EPIPE — which for a Rust binary was a panic (F-110). The negative form one step earlier (`... && fail "project still listed after delete"`) has the same hole in the other direction: a listing that failed passes it, so a broken `nemr list` first became visible at the assertion that fired. **Intermittent:** the same commit, re-run unchanged, passed — which rules out the shape it was first read as, a restore path producing something `list` cannot see, since that would fail every time. It does not reproduce on the developer host, including in CI order. **Five mechanisms were investigated and all five refuted as the cause,** each with commands and controls: daemon identity churn, containerd visibility, residue from earlier steps of the same job, worker starvation, and the EPIPE race. The last of those is the correction worth keeping. It is REAL — reproduced independently three times (17/200 and 19/200 under load, with the panic captured) and it has exactly the load-dependent shape of one attempt failing and the next passing — but its precondition is at least one stdout line AFTER the matched row, and matching the last row measures 0/200. In that CI job `sync-acc-…` WAS the last row: `strace` shows the final `write(1, …)` is the last project row, the untracked-volumes block is guarded by `if !untracked.is_empty()` and appears nowhere in the job, and every other project that job creates (`e2e-smoke-`, `port-acc-`, `netns-acc-`) sorts before it. The direction is backwards too — the mechanism makes the developer host vulnerable and CI immune, and the observed facts are the opposite. Timing closes the rest: **8.04 ms** between `pull`'s last line and the FAIL, and autostart polls in 100 ms steps, so that `nemr list` reached a live daemon and returned fast without hitting any timeout. *(A trap for anyone measuring this: `grep` inside some tool shells is a function that execs `ugrep`, whose `-q` does NOT exit on first match, so the race silently cannot reproduce there. Two independent measurements were wrong before this was noticed.)* | **Made diagnosable, and the class removed.** `output_has` in `scripts/lib/proc.sh` runs the command, checks its status FIRST, and on a failure prints the status, the stderr and the stdout and exits — a broken instrument is never read as a measurement. There is no pipe, so neither `pipefail` nor EPIPE can turn an answer into a failure: same question, same load, 200 runs — **19 false negatives through the pipe, 0 through the helper.** Its own test asserts all three outcomes and carries a CONTROL that reproduces the old conflation on the same input. Migrated: both `sync_acceptance.sh` sites and `e2e_smoke_test.sh`'s "project gone from list". The restore step now asserts the project is USABLE — listed, running, and resolving DNS inside the session — not merely listed. CI dumps the daemon log, `nemr list`, containerd's records and rootlesskit's links and NAT rules when the host job fails; `nemrd.log` held the daemon's side of every failed command all along and was simply never collected. **The cause remains open.** The strongest surviving candidate is a transient error inside `nemr list` itself — `project::list` propagates `task_state`'s error for every container with `?`, which is fast, intermittent, and lands on the stderr the assertion threw away. If it recurs, it now names itself. |
| **F-108** | ~~note~~ **RESOLVED** | NET-02/NET-05 | **NET-02 separated port spaces, not sessions: every session could reach every other session's address and ports.** Raised by the NET-02 adversarial review, recorded as a scope statement, then ruled by the Product Owner: *"its own network namespace reads as a security boundary, and someone will eventually put two clients' work in two sessions on that assumption. I'd rather have the honest claim than the accurate caveat."* | **RESOLVED (2026-08-29).** Established by measurement before building, on two disposable sessions: A reached B and B reached A, both returning the other's body. **One `FORWARD` rule over the whole `10.99.0.0/16` range** closes it — a per-session bridge would not, because sessions are ROUTED to one another inside rootlesskit's namespace. Measured with the rule: A→B and B→A both refused, while egress from both sessions and both host→session forwards still worked, and removing the rule let the same request through again — the MASQUERADE control's shape. The rule is range-wide policy, not per-session state, so teardown does NOT remove it; it is re-asserted on every start instead, because rootlesskit's network namespace is destroyed when rootless containerd restarts and takes every rule with it, so "install it once" would silently stop being true. Inserted at the head of the chain, since appending would sit behind any ACCEPT another tool had added. **One limit, stated rather than caveated away:** a port published with `--expose` binds `0.0.0.0` and is thereby published to the network, which includes other sessions. My flanking probe (session → the host's address → another session's forwarded port) returned FAIL, but on this host that proves less than it looks — slirp's own subnet is `10.0.2.0/24` and the host's address is `10.0.2.15`, so the packet never left the slirp link. NET-05 says so plainly instead. `isolation_is_active` reads the chain and never repairs it, so it can serve as a control. |
| **F-107** | med | NET-02 | **Three teardown paths that removed a session's network at the wrong time or not at all.** (1) `stop()` disconnected the veth and the NAT rule whether or not `stop_task` had succeeded, so a task that refused to stop lost its address and its egress while still running and serving. (2) `disconnect_session` discarded its exit status, its stderr and the result of entering rootlesskit at all, so the one failure that leaves every rule behind — not being able to enter the namespace — was indistinguishable from a clean removal with nothing to do. (3) `tests/common::purge` goes straight at the host by design (the engine's own delete may be the thing under test), and therefore never released the session network: every purged host-backed test leaked a veth and a MASQUERADE rule, which then failed the NET-02 acceptance's teardown check for reasons unrelated to the code under test. | **All three fixed.** `stop` disconnects only when the task actually stopped. `disconnect_session` still tolerates absence but now greps for anything of its own left behind and reports it, and reports a failure to enter the namespace as such. `purge` reads the allocation label before deleting the record and releases the network directly, keeping its straight-at-the-host property. |
| **F-106** | med | NET-02 / F-99 | **Session-network allocation was an unlocked read-modify-write, and a failed read of the allocations meant "nothing is allocated".** `allocated_indices` ended in `unwrap_or_default()`, so a containerd read error handed the next project index 0 on top of a live one; and the index was chosen from a snapshot and published only later by `create_container`, with several containerd round-trips in between, so two concurrent creates could take the same index. Neither failure errors: both projects derive the same `/24`, the same addresses and the same link name, the loser starts with no network, and its host forwards point into the winner's session. This is F-99's shape — deferred by ruling to this pass — with a second instance added rather than the first closed. | **Fixed.** The read propagates its error. Allocation is serialised on a process-wide lock held from choosing an index until it is visible on a container record — the point at which it is actually taken. The start-path migration re-reads under the lock, so two starts of the same project cannot take two indices. Process-wide is exactly as strong as the daemon's own claim to be the single writer to containerd; if that stops being true this needs a lock in containerd, which is what F-99 remains open for. |
| **F-105** | **high** | NET-02 | **The route-conflict guard had two blind spots, and its tests omitted precisely them.** (1) `overlaps_our_range` compared the second octet to 99, so it answered "no" for every aggregate between /9 and /15 — `10.0.0.0/9`, `10.64.0.0/10`, `10.98.0.0/15` all contain `10.99.0.0/16` — and a summarised route is exactly the form a corporate VPN pushes, which is the case the guard was written for. (2) `check_range_is_free` ignored `ip`'s exit status, so a routing table it could not read was indistinguishable from one with no conflict: the guard silently became a no-op. It also read only the main table, and took the first whitespace token as the destination, which reads `unreachable 10.0.0.0/8` as a destination called "unreachable". A guard that defaults to accepting is worse than no guard, because it is believed. | **All three fixed and each proven red.** Prefix arithmetic replaces the octet comparison (a default route is excluded — it contains everything and conflicts with nothing). A non-zero `ip` status is a refusal with the reason. `table all`, and route lines are read past their type keyword. The whole-table decision is split into `conflicting_route` so it is testable against a real table — it had no test at any level before, which is how it carried two blind spots at once. Mutation-checked: restoring the octet version fails 4 tests, restoring the token-zero read fails 4, and the two tests that shipped with the defect stay green under both. |
| **F-104** | **high** | NET-02 | **A wiring that failed halfway left a link behind, and the next start mistook that link for a wired session.** `connect_session` began `if ip link show nemrN >/dev/null 2>&1; then exit 0; fi`. A link with that name is not evidence that a session is wired — a failure after `ip link add` leaves exactly that — so every leaked link turned into a session that starts, reports success and has no network at all. The peer end was the constant `ceth0`, created in rootlesskit's SHARED namespace, so one leaked peer also made every OTHER project's `ip link add` fail with `RTNETLINK answers: File exists`, naming a project that had nothing wrong with it. The comment claimed "the caller tears the session down"; the caller stopped the task, which does not remove links that are no longer in the task's namespace. And the whole script asserted success on the strength of its commands rather than the outcome. | **Fixed.** The wiring starts by deleting the pair, which removes both ends wherever they are — a freshly started task's namespace is empty by construction, so rebuilding is what idempotence means here. A cleanup trap removes the pair on any failure. The peer is named per session (`nemrcN`) while it is in the shared namespace and renamed to `ceth0` inside, so no two sessions can collide and a leak of either end is visible to the teardown check. `iptables -w 5`: the xtables lock is shared with rootlesskit's port driver and with podman. And the script now reads the address and the default route back from inside the session before reporting success. |
| **F-103** | **high** | NET-02 / NET-04 | **Every project created before NET-02 was upgraded into one that starts, reports success and cannot reach anything.** `start` wired the session only if `find_container(...).ok().and_then(|c| allocation_from_labels(&c.labels))` produced an allocation. A project with no `nemr.netns` label — which is every project that existed before this pass — took the `None` branch and was skipped in silence, while its task still received its own empty network namespace from the OCI spec: no address, no route, loopback down. Failing to wire was fatal; not knowing whether to wire was silent, which is exactly backwards. The same `.ok()` also swallowed a containerd read error into the same silence. | **Fixed.** The container read is hard — a failure to read is a failure to start. A project with no allocation is allocated one, under the lock, and it is recorded, so "not allocated yet" is resolved rather than treated as "no networking wanted". Pinned by a host-backed test that strips the label, starts the project, and asserts both that an allocation was recorded and that the session's namespace really carries its address and its default route. Proven red by restoring the skip. |
| **F-102** | med | NET-02 / testing | **The only automated NET-02 test asserted a property the OCI spec provides on its own.** `net02_a_session_has_its_own_network_namespace` compares the session's `net:` inode against rootlesskit's. That is worth pinning — it is the inverse of the retired NET-01 — but it passes with every line of `engine::netns` disabled: the namespace still exists, still differs, and still reaches nothing. Isolation was never the deliverable; a session that WORKS while isolated is. The four-field forward spec (`parentIP:parentPort:childIP:childPort`) and `via_session` had no test either, and the IFNAMSIZ test could not fail for any input a `u8` permits. | **Fixed by testing the wiring, not the isolation.** `net02_a_started_session_is_wired_not_merely_isolated` reads the session's own namespace from outside and requires its address and default route, with a control that fails if the namespace could not be read at all. Demonstrated rather than argued: with wiring disabled, the namespace test still passes and the two new tests fail. The interface-name test now pins uniqueness across all 256 indices, which the shared `ceth0` peer name violates. |
| **F-101** | note | NET-02/NET-03 | **Host-port collision framing, now that container ports are no longer shared.** Recorded at the Product Owner's request rather than designed. Before NET-02 a host-port collision and a container-port collision were the same shortage: one namespace, one set of ports. They are now different things. **Host** ports remain genuinely exclusive — they are the host's — so `project_holding_host_port` and its "declared by project X" message stay correct and useful. What disappears is the R-07 framing underneath: two sessions wanting *container* port 8000 is no longer a conflict at all, it is the normal case. The open question is whether the collision message should now say so — a user told "host port 8000 is already declared by project web" may reasonably wonder why, when they have just been told sessions are isolated. | **Noted, not designed** (ruling: look at it once the namespace work lands). No code change; the current messages are accurate, just framed for a world with one shared port space. |
| **F-100** | med | NET-03 | **The live-forward parser was positional, so it broke for reasons unrelated to what it parses.** `rootlessctl list-ports` prints `CHILDIP` blank when unset, giving a row with one fewer field, and the parser read columns by index — so the host port was read as the container port. Nothing errored; the values were simply wrong, and a forward that cannot be matched cannot be torn down (this was the mechanism behind half of F-98). A parser correct only for the layout in front of it is the F-80 shape. | **Fixed structurally rather than hardened:** `rootlessctl list-ports --json` emits newline-delimited objects where an absent `childIP` is *absent* rather than blank, so the column-shift failure cannot recur, and an unparseable line is visibly unparseable instead of silently misread. `LiveForward` gains `container_ip`, which NET-02 needs anyway. Verified against the real `rootlessctl` on this host, not only against a fixture. |
| **F-99** | med | NET-03 | **`add_port` is a non-atomic read-modify-write on the container label.** *(2026-08-28, measured while closing F-106: `update_container_labels` sends the whole map under a bare `labels` field mask, which containerd applies as a REPLACE — so a racing writer does not merge, it overwrites. Three callers do read-modify-write against a map fetched by an earlier `list_containers`: `set_agent`, `add_port`, `remove_port`. The session-network instance is closed by F-106's lock; these remain, deferred by ruling.)* It reads the whole label map, modifies the ports entry and writes it back, with no compare-and-set. Two concurrent `nemr port add` invocations can therefore clobber one another's declaration, and the collision check is not atomic with the write either. Raised by the WP-M adversarial review and confirmed. | **Deliberately deferred to the NET-02 pass** (Product Owner ruling), which rewrites this surface: fixing it twice is worse than fixing it once in the code that survives. Tracked here rather than living in a commit message. Requires concurrent CLI use to trigger, which is why it is not urgent — but it is real, and it is not forgotten. |
| **F-98** | **high** | NET-03 | **An unvalidated bind address exposed sessions to the network while every surface said loopback — and made the forward impossible to tear down.** Found by adversarial review of WP-M (58 agents; 21 findings confirmed of 52 raised, 20 refuted). `parse_port_arg` took the address slot verbatim. So `nemr port add web :9000:8000` produced an empty address, which rootlesskit binds as `*:9000` — **every interface, dual-stack** — with no `--expose`, no warning, and no "(exposed)" marker, because exposure was the string test `host_ip == "0.0.0.0"`. A real NIC address (`10.0.2.15:9000:8000`) was LAN-reachable and equally silent. Verified live on this host by the verifying agent before the fix. Worse downstream: an empty address makes rootlessctl's table four columns instead of five, so `parse_list_ports` misread host port as container port, and since every teardown matches on host port, the wildcard listener **survived stop, `port rm` and `delete`** — breaking NET-03's derived-state rule. Separately, `localhost:…` and `0:…` were accepted, stored, and then refused by rootlesskit forever, leaving a permanently unbindable declaration that still reserved the host port against other projects. Also confirmed in the same review and fixed here: port failures at `start` reached nobody (an untagged `tracing::warn` the audit layer drops) while `status` kept advertising the dead URL; `host_port + 1` panicked in debug at 65535; every pre-flight bind failure was suffixed "by something else on this machine", mislabelling a PRIV-01 permission failure; and `apply_one` advised `nemr reconcile` for a conflict reconcile provably will not touch. | **Fixed.** The address is parsed as an `IpAddr` and normalised, refusing empty, `localhost`, `0` and ambiguous leading-zero forms (a classic way to smuggle one address past a check reading another) with a message naming the accepted forms. **Exposure is now "not loopback"**, not equality with one literal, so any routable bind is reported as exposed — and the CLI warns on the bind that actually **resulted**, after it succeeds, which also closes the two paths where the old flag-gated warning was wrong (fired for adds that failed; silent for the explicit-address opt-out the parser documents as beating the flag). Start failures are tagged `nemr_audit = "warning"` and carried to the user by the audit stream; `PortSpec.live` distinguishes declared-from-bound so a dead declaration is no longer printed as a working URL. Overflow made saturating; the conflict suffix applied only to a genuine `AddrInUse`; the reconcile advice corrected. Six new guards, each proven red under a **compiling** mutation — an earlier red-check attempt produced code that did not build, which proves nothing and was redone. |
| **F-96** | med | NET-03 | **`nemr delete` left its port forwards bound, and a stopped project accepted a port the host already held.** Found by verifying cleanup rather than assuming it (standing rule) and by re-testing a collision against a *genuinely* held port after the first attempt proved nothing. (1) Forwards live in rootlesskit, not the container, so deleting a project left a host port bound to nothing — and a later collision would name a project the user could not find. (2) `add_port` only bound when the project was running, so declaring a port on a stopped project succeeded even when the host already held it, deferring the failure to `start` where it is a warning nobody reads. | **Both fixed.** `delete` withdraws forwards before removing the container; `reconcile` gained a stale-forward sweep for the same shape arriving by other routes (a crash, a manual `rootlessctl add-ports`), proven against a planted orphan. `add_port` on a stopped project performs a pre-flight test bind, reporting the host holder immediately with a concrete alternative command; a control confirms a free port on the same stopped project still succeeds, so the check is not simply refusing everything. |
| **F-95** | med | harness | **Seven wait loops that could not explain their own failures — and the seventh was written after the rule against it existed.** The recurring shape: background a process, poll a fixed number of times, and on failure report the symptom only — "did not come up", a log captured and never printed, or printed when empty so it read as though no log existed. Each instance was fixed individually and the rule was recorded in `.claude/loop.md`; the seventh instance (in `sync_acceptance.sh`, authored *after* that rule) is what established that a checklist competes with the moment of writing and loses. | **Fixed structurally.** `scripts/lib/proc.sh` provides `wait_for_service` (aborts the instant the process dies rather than burning the window; distinguishes alive-but-silent from exited-with-status-N; prints the log with a header and per-line prefix, saying "(empty — wrote nothing)" or "(no such file)" rather than printing nothing), `wait_for_ready` for dependencies we did not start, and `require_tcp` for probing one dependency by name with its remedy. 18 assertions in `scripts/test_proc_lib.sh` assert on what the helper **says**, not only its exit status — a wait helper that returns 1 without explaining is the defect it exists to end. **`scripts/check_wait_discipline.sh` is what makes it non-optional**: it fails the build when a script captures a backgrounded PID without calling `wait_for_service`, and its control shares the real predicate (so the control cannot drift green against a re-implementation) and discriminates three ways — hand-rolled loop caught, compliant script not, background-free script not. Both run in CI. `sync_acceptance.sh` migrated and verified three ways: the acceptance still passes; the DB-unreachable control still names Postgres and its remedy; and a genuinely broken server now surfaces `Error: NEMR_BUNDLE_DIR is required for the local backend` from the captured log — the exact class of cause that used to be invisible. Four PID-less loops in host-provisioning scripts are **deliberately not migrated**: the gate does not cover them and this machine cannot exercise those paths without a clean host, so changing them would trade a working diagnostic for an unverified one. |
| **F-94** | **high** | NFR-01 / CI | **The Docker-freeness gate could not see a CI service container — it caught a description of Docker while missing three literal `docker` invocations in a workflow it was already scanning.** The gate scans `.github` precisely to catch Docker in CI. Its pattern is `docker <verb>`; a `services:` block names no command, so the `services: postgres` added in WP-J passed every run since. What finally fired the gate was a *comment* explaining how service containers start — prose about the dependency, while `/usr/bin/docker pull postgres:16`, `docker create` and `docker start` ran three jobs above (established from our own job logs, not inferred). A guard that catches descriptions of the thing but not the thing: the F-56 family, third variant after F-80. | **Fixed.** New section 3b keys on the shape that *requests* a container (`services:` / `image:`) rather than on the word "docker", with a self-contained two-way control (a services block is caught; a workflow with no containers is not) — without it this check could degrade into the same silence it exists to end. The dependency itself is removed: both jobs now bring Postgres up under rootless podman via the script developers already run locally. E-17 rules the scope question the finding exposed. **Process note, recorded because the shape recurs and nearly won:** the first response to the gate firing was to reword the comment so it stopped matching. That kept the build green and made a real finding invisible; it was recoverable only by re-reading the edited prose. When a constraint check fires, the first question is what it found, not how to make it pass — and widening is always safe where narrowing needs a ruling. |
| **F-93** | med | CI | **The host job cannot use a GitHub service container, because provisioning the Docker-free stack kills it.** The WP-K acceptance needs Postgres, so a `services: postgres` block was added to the host job. It failed with "no Postgres listening on localhost:5432" — *after* the provisioning step had already reported success. Cause: service containers are started by **Docker**, Docker runs on the **system containerd**, and `ci_provision_host.sh` disables `containerd.service` (PRIV-01 — a socket-resolution mistake must not be able to reach a root daemon). So the job pulls the rug from under the container serving its own database. NFR-01's posture and GitHub's service model are genuinely incompatible **within one job**; the `sync-tests` job is unaffected because it never provisions the rootless stack. | **Fixed — remedy superseded, see below.** First fix was `scripts/ci_provision_postgres.sh` (Postgres from the Ubuntu archive). **Superseded by rootless podman** (E-17/F-94): podman is daemonless and does not use the system containerd, so it survives the very `systemctl disable containerd` that killed the service container — and it is the same `scripts/setup_sync_test_db.sh` developers run locally, so CI and local became one mechanism instead of three. The apt script was deleted rather than left as a second way to do this. It runs **after** host provisioning (nothing it installs depends on containerd), waits for `pg_isready` rather than trusting the unit's exit, creates the role and database idempotently, and **verifies the exact connection the tests use** before returning. The `services:` block is gone from that job with a comment saying why, so it is not re-added. Found only because the acceptance's failure diagnostic had just been fixed to probe the database separately — the previous message ("server did not come up", empty log) would have sent the next reader hunting a server bug. |
| **F-92** | **high** | WP-K | **Adversarial review of the sync client confirmed six defects, five of them shipped-quality.** A 5-reviewer + 23-verifier workflow over the new surface; 18 findings survived refutation, of which these were fixed. (1) **Duplicate heartbeat holders**: `pull` spawned unconditionally where `push` guarded, so push-then-pull left two detached holders on the same (holder, fence). The server matches heartbeats on holder+fence with no notion of process, so both renewed forever while nothing referenced the first — a leak per pull, and a later `release` killed only the one the state file named, leaving the orphan to keep the lease alive and re-create the state file it had just cleared. (2) **Stranded lease**: when a re-acquire advanced the fence while the previous holder was still alive, the fence-blind "spawn only if no live holder" guard saw a live process and skipped — so the NEW fence was recorded nowhere, nothing heartbeat it, and a later release sent the stale fence, was refused, and told the user the lease was released while the server held it until TTL. (3) **Wrong-process signal**: holder identity was a substring test over the joined `/proc` cmdline, so with PID reuse a release of `proj` could SIGTERM the holder of `proj-backup`. (4) **Heartbeat pacing** inferred from `expires_at - now` collapsed toward a hot loop on a partly-elapsed lease. (5) **The plaintext bundle was world-readable**: `tempfile::tempdir()` honours the umask — measured 0775 dir / 0664 file on the reference host — so the decrypted session, the exact thing E-16 protects, sat readable by every local user for the length of a push or pull. (6) **Off-PATH exec**: a subcommand name containing `/` made `nemr ../evil` run a binary from a path PATH never sanctioned. | **All six fixed.** `ensure_holder` is idempotent per fence (reuse the live holder, retire any other) and `pull` no longer spawns blindly; `ensure_lease` retires the old holder before taking a new fence, and push records the fence unconditionally; identity matches **exact argv elements including the fence**; the server now reports `ttl_seconds` and the client paces off it, with the floor clamping the *interval* not the TTL (clamping the TTL ate the 3× margin and let a live lease lapse under load — caught by a test); the plaintext tempdir is chmod 0700 before anything is written into it; a `/` in a subcommand name is refused. Five guard tests, each **proven red under its true mutation with the mutation verified to have applied** — an earlier round of these tests passed under mutation (green over nothing, the F-56 shape) and was rewritten. Server-side, upload re-checks the fence *inside* the metadata UPDATE as defence-in-depth against a takeover landing between check and write; **that guard is deliberately not claimed as tested** — the pre-flight check masks it and only a test-only pause hook in the request path could isolate it, which is a worse thing to own. Recorded rather than asserted. |
| **F-91** | low | harness | **The regression suite took 188s against a usual ~30s at WP-J closure.** Raised by the Product Owner with the right instinct: F-78 established that load changes behaviour here, so it should be measured, not assumed. The suspected cause (the new Postgres container competing for CPU) was measured and **exonerated**. | **Measured, cause identified, no code change.** Controlled runs on an idle host, test binary pre-built: pg stopped **26.3s**, pg running idle **28.7s** — the container costs ~2s. Reproducing the closure scenario — the suite run **while a workspace release rebuild saturates the cores** — gave **122.5s** test-time (229s wall), the observed inflation. Cause: build/test overlap during closure, not the container. Remedy is ordering, not code: let `install_engine.sh`'s build **finish** before starting the suite. Bonus confirmation: mid-rebuild, the F-90 freshness gate correctly refused the momentarily-stale binary — the gate works under exactly the conditions that used to blur closure. |
| **F-89** | low→ | WP-J | **The pre-login KDF-params endpoint was an account-enumeration oracle.** `/v1/auth/params` is unauthenticated; it 404'd an unknown email and 200'd a known one, so anyone could test whether an address had an account. Low severity while there is one user; it changes the day the server is public, and it is the kind of thing that ships and stays. A second oracle of the same class sat on `/v1/login`: a wrong password ran Argon2id (slow) while a nonexistent email returned fast, leaking existence by timing. | **Fixed (both).** An unknown email now returns a **deterministic pseudo-salt** — `HMAC(pepper, email)`, 16 bytes — and default params with the same 200 shape as a real account; the pepper (`NEMR_AUTH_PEPPER`, or random per process) keeps it unpredictable so an observer cannot recompute it, and deterministic so repeat probes match a real account's stable salt. The login miss now spends one Argon2id too, equalising timing. Guard test `kdf_params_does_not_reveal_whether_an_email_exists` asserts same-status/same-shape and goes red if the endpoint 404s an unknown email. |
| **F-82** | low | tooling | **`gh run view --log-failed` returns empty for a step that failed with a full log.** Observed while retrieving the publish digest: the step exited 1 and had complete output, and `--log-failed` printed nothing; the whole log had to be fetched instead. F-65's shape one layer out — in the GitHub API/CLI rather than in this project's scripts — and it will recur on the next CI failure. | **Recorded, not fixable here.** Noted so the next person debugging CI does not conclude "no output" means "no diagnostic". Use `gh run view --job=<id> --log` and search it, rather than trusting `--log-failed`. Compounded once already: a panic was invisible because CI printed only `tail -40` (fixed in the previous pass), and then invisible again because `--log-failed` returned nothing. |
| **F-28** | med | VOL-06 | **`ensure_volume_mounted` accepted any filesystem at the mount point.** Tracked since the original audit and unexamined until now. `is_mounted` asks "is something mounted here?"; nothing asked whether it was *this project's* volume. A mount landing on the wrong path — a live possibility whenever many volumes are attached and released concurrently — was accepted silently and the container ran against a foreign filesystem. | **Fixed, and it is the leading explanation for F-63a.** The captured failure showed `mounted=true`, a real loop device, and a volume containing only `lost+found` — a filesystem that was not this project's. `start` now resolves the mounted filesystem's backing image from mountinfo plus `/sys` and refuses unless it matches, refusing outright when the mount is not loop-backed. The decision is a pure function (`mount_identity`) so the guard tests the production path rather than a copy; proven red both by accepting a mismatched image and by dropping the loop-major check. **Not yet established** that F-28 *is* F-63a's cause: the failure capture now prints the mounted image alongside the expected one, so the next occurrence says so directly. |
| **F-67** | med | harness | **WP B's tracing was discarded in the regression suite.** Nothing under `tests/` ever initialised a `tracing` subscriber — that happens in `nemr`'s `main` — so `NEMR_LOG=... cargo test` produced no engine logs at all. The structured lifecycle logging added specifically to make failures diagnosable was absent from the one place failures get diagnosed. Found while diagnosing F-63, where `ensure_volume_mounted`'s decision-point log was the fact needed and was not there. | **Fixed:** `common::init_tracing()` installs a test-writer subscriber when `NEMR_LOG` is set, off otherwise. Confirmed by the engine logs now present in the captured F-63 failure. |
| **F-64** | med | CI | **A CI job named for a check it never ran.** The lint job is titled *"clippy (warnings denied) + fmt"* and installs the `rustfmt` component, but has no `cargo fmt --check` step in either workspace. Twenty-six files were unformatted across the engine and the privileged helper, and every PR to date reported this job green. Same class as F-60: the signal was real, what it signalled was not — and here the job's own *name* was the false claim. | **Fixed:** `cargo fmt --all -- --check` added for both workspaces, and the tree formatted (mechanical, reviewed by hand for the helper). Verified by observing the step fail on the unformatted tree before formatting it. |
| **F-65** | high | harness | **Every failure diagnostic in the verification harness was unreachable.** `output=$(cargo test ...)` followed by `status=$?` aborts at the *assignment* under `bash -e` — GitHub's default step shell, and `set -euo pipefail` in `verify_wp_a.sh`. So `status=$?` never ran and the `tail -40` beneath it was dead code in all three sites: both CI test jobs and `scripts/verify_wp_a.sh`. A red suite printed cargo's exit code and nothing else. Found by hitting it: the PR's regression job failed and its log contained no test output at all. The verification script the Product Owner runs would have died silently on exactly the failure it exists to report. | **Fixed:** the assignment moved inside an `if` condition, which is exempt from `-e`, at all three sites. Proven by running the old block and the new block against a stand-in that fails with a known marker line: old printed nothing and exited 101, new printed the full diagnostic and exited 1. |
| **F-66** | med | harness | **The E-11 offline guarantee has never been verified in CI, and the refusal did not say why.** `e11_export_and_import_work_with_no_network_and_no_credentials` was written after PR #11 merged, so this branch is the first time it has ever run on a runner — where it refused: Ubuntu 24.04 ships `kernel.apparmor_restrict_unprivileged_userns=1`, which blocks the `unshare -rmn` the test needs. The refusal is correct behaviour (a skipped guarantee is not a verified one), but it reported only "namespaces are unavailable", so the cause had to be guessed from outside. | **Fixed:** `ci_provision_host.sh` enables unprivileged user namespaces on the runner and prints the probe result either way; the probe now carries `unshare`'s own stderr plus the sysctl's value. `the_namespace_probe_reports_why_it_failed` runs a stand-in that fails like `unshare` and asserts both details survive into the message — proven red against the bare-boolean version. Scope note: the sysctl is relaxed only on a disposable CI runner, and the privileged helper's scope is untouched. |
| **F-62** | high | harness | **The engine binary had no hash gate.** The privileged helper has been gated on installed-matches-source since F-58; `nemr` — the binary a user actually runs, and the one `scripts/e2e_smoke_test.sh` invokes off `PATH` — was not. A smoke test could pass against a binary built from a commit that no longer exists and be reported as closing a milestone, which is precisely what the closure rule ("merged **and** reinstalled from that commit **and** verified against the installed artifacts") is supposed to prevent. Two sub-findings surfaced while fixing it: `cargo install --path .` cannot be hash-gated at all (it rebuilds in its own target directory and emits a binary differing by ~40 bytes from `target/release/nemr` built from identical source), and `run_offline` hardcoded `target/debug/nemr`, so `cargo test --release` would have run a stale debug binary. | **Fixed:** `installed_engine_matches_built()` checks hash **and** source-mtime across `src/`, `crates/` and the workspace manifests; `the_installed_engine_matches_its_source` enforces it and the smoke test calls it before touching `$NEMR`. Install is now `scripts/install_engine.sh` (build once, copy the artifact — the same property `setup_test_host.sh` gives the helper). `run_offline` uses `CARGO_BIN_EXE_nemr`. Proven red four ways: hash mismatch, source newer than install, staleness in a *linked crate* rather than `src/`, and not-installed — plus a green control. |
| **F-61** | med | harness | **The M12 bucket acceptance was unfalsifiable.** The round-trip deleted the object on every exit path, so the only evidence a run ever produced was its own summary: an operator who did not trust the tool had nothing left to inspect. In a project whose standing rule is that a green signal must be provable, the acceptance harness was the one thing taken on faith. Fixing it exposed a second defect: cleanup deleted the object even when the pre-flight control tripped, i.e. when the run had written nothing and the object belonged to someone else — the VOL-05 shape, a wrong action under a truthful-looking summary. | **Fixed:** `--keep` leaves the artifact and prints key, size, server ETag and local SHA-256 with instructions to verify without the tool; `--local <dir>` runs the identical acceptance with no credential so the harness can be checked before it is trusted with one; cleanup moved into the library so it is tested rather than only run live; `PreexistingKey` makes the pre-flight failure distinguishable so cleanup never deletes a foreign object. Guards proven red three ways (cleanup-only-on-success, `--keep` ignored, foreign-object deletion). Both failure paths exercised through the real binary. |
| **F-59** | med | harness | **HOME near-miss (caught before shipping).** The first E-11 offline test pointed `HOME` at an empty directory to hide the credential. `HOME` also derives the volume tree, so the test would have "proved" export fails without credentials when it actually could not find the volume — a right-looking test measuring something adjacent, the same class as the `/var/lib/containerd` miss. Replaced with a mount namespace that hides `~/.claude` only, leaving volume paths intact. | Recorded; the working test carries a control asserting the credential exists outside the namespace. |
| **F-58** | high | multiple | **Guard-test audit: 10 confirmed F-56 siblings.** A 10-agent adversarial audit applied the new rule ("would this test pass with the guarded code deleted?") across the suite. Four rated *guards nothing outright*: the kernel-5.8 floor test compared tuple literals to tuple literals; the extraction-ordering test re-implemented the production sort and asserted on its own output; the schema-refusal test called the helper directly while `open()`'s call could be deleted; the F-54 filter tests exercise a code path no real export reaches. Six weaker: fault-injection accepted any error, the helper-hash gate is not source-aware, `mountinfo_device_for` tested one optional-field count, and others. | **All 17 closed.** Each fix was verified by disabling the guarded code and watching the test go red, then restoring it. Four tests were deleted rather than fixed (tautologies asserting a fall-through default, or a constant production no longer consulted) and their intent folded into real-layout tests. The dead `.claude.json` export wiring was removed rather than left looking like a working defence. |
| **F-57** | high | D-02 / F-54 | **Secret-absence guards degraded with size.** Four tests asserted "the secret must not appear in the bundle" by grepping the raw `.nemr` bytes. Chunks are zstd-compressed, so a marker is findable only while content is small enough to store near-verbatim — measured: visible in a 34 B bundle, **invisible in a 241 KiB one**. The guards therefore passed on fixtures and stopped guarding at realistic sizes: they degrade exactly when it matters. A direct F-56 sibling, found by applying the new guard-test rule. | **Fixed:** all four assert on *extracted plaintext* via a shared helper; each carries a positive control; the credential guard was proven to go red with the filter disabled and green with it restored. |
| **F-56** | high | D-02 / M9 | **Guard test guarded the wrong thing.** `credentials_never_travel_under_any_policy` passed while asserting on `root/.claude/.credentials.json` (the container's path); a real export walks the volume, whose layout is `.nemr-state/…`, so the checked path cannot occur. D-02 held **structurally** (credential is a host bind-mount), not because the test enforced it — false assurance, invisible because green. The sixth instance of the recurring green-over-wrong pattern and a new variant. | **Fixed in M9:** filter matches the credential filename anywhere; tests assert against the measured layout. Rule added to `.claude/loop.md`; siblings audited. |
| **F-55** | high | F-54 / M9 | **MCP configuration cannot travel.** F-54 rules that it must, but `.claude.json` lives on the **rootfs**, not the portable volume, so a volume-only export cannot reach it. `filter_claude_json` is correct and currently unreachable in a real export. | Needs an M8 bind-mount change (relocate a portable `.claude.json` onto the volume), which invalidates D-06 until re-verified. **Raised for the M9/M10 review.** |
| F-28 | med | VOL-06 | `ensure_volume_mounted` accepts *any* fs at the mount point (identity not checked) | verify the mount is backed by the project's loop device |
| F-35 | med | PROC-04 | exec-kill on stop untested | add regression test |
| F-12 | high | AUTH-02 | Credential file bind-mount pins an inode → host rotation invisible in-container | WP-C follow-up (mount a dir, or re-resolve) |

**Open — deferred to WP-B/C/D (tracked, not lost)**

| ID | Sev | Requirement | Finding | Owner |
|---|---|---|---|---|
| F-45 | low | NFR-02 | create/start latency measured nowhere | WP-B benchmark harness |
| F-22/F-34 | med | VOL-05 | `list` trusts labels blind; destroyed volume shown as merely "unmounted" | WP-A/B follow-up |
| F-25 | med | NFR-01 | `BASE_IMAGE` in a squattable Docker Hub namespace | pin by digest (WP-B reproducible build) |
| F-46 | low | PROC-02 | `std::process::exit` in attach/delete skips destructors | WP-B error-model unification |
| F-49 | low | NFR-01 | `~/.docker` created by buildctl's vendored telemetry (not Docker) — audit tripwire | document |

**Refuted / no-change** (12 findings): kept in the audit record; not reproduced
against the code (e.g. names are validated in two places, killing several
weird-name scenarios). Available in the workflow journal.

---

## Maintenance

Update this file in the same commit as any change that adds, moves, or resolves
a requirement or finding. The regression suite plus `nemr reconcile` are the
runtime guards; this document is the map. See `.claude/loop.md` for the
regression cadence.
